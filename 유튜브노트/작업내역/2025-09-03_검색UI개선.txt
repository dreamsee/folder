작업 날짜: 2025-09-03
작업명: 검색 결과 UI 개선 (더보기 버튼 및 시청 횟수 표시)

구현 내용:
더 많은 영상 보기 버튼을 안본 영상에서만 표시하고, 시청 횟수를 1회부터 표시하도록 개선

수정 파일 및 변경 내용:

1. client/src/components/VideoLoader.tsx

   수정됨:
   - 더 보기 버튼 조건: {nextPageToken && !isLoadingMore && ( → {nextPageToken && !isLoadingMore && filterMode === 'unwatched' && (
   - 시청 횟수 표시 조건: watchInfo.watchCount > 1 → watchInfo.watchCount >= 1

삭제된 기능:
- 모든 필터에서 더 보기 버튼 표시
- 2회 이상 시청한 경우만 시청 횟수 표시

추가된 기능:
- 안본 영상 필터에서만 더 보기 버튼 표시
- 1회 시청부터 시청 횟수 표시